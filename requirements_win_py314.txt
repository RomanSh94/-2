# X20 Bot — Requirements for Windows + Python 3.14+
# Python 3.14 имеет очень мало готовых .whl файлов
# РЕКОМЕНДУЕТСЯ: Используйте Python 3.12 или 3.13 вместо 3.14

# Если вы всё ещё используете Python 3.14, некоторые пакеты могут не установиться.
# В этом случае установите Python 3.12 (или 3.13):
# https://www.python.org/downloads/release/python-3122/

# ОСНОВНЫЕ ПАКЕТЫ (совместимые с Python 3.12+)
aiogram>=3.6.0,<4.0
openai>=1.3.0
python-dotenv>=1.0.0
aiosqlite>=0.19.0
apscheduler>=3.10.0
flask>=3.0.0
aiofiles>=23.0.0

# ДЛЯ EMAIL УВЕДОМЛЕНИЙ (опционально, можно пропустить)
# aiosmtplib>=2.0.0  # Может требовать компиляции на Python 3.14
# Альтернатива: используйте встроенный smtplib (синхронный)

# ДЕТЕКТИРОВАНИЕ ЯЗЫКА (опционально)
# langdetect>=1.0.9  # Может требовать компиляции
# Альтернатива: встроенное определение языка в language_detector.py (работает без зависимостей)
